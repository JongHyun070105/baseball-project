# ADR 0001: Authoritative pure TypeScript domain

The pure TypeScript domain owns career and match truth, deterministic random streams, ball flight, collision classification, commands, events, and replay hashes. React, R3F, Three, and Rapier are presentation/application adapters only. Any persisted contract change requires another numbered ADR, schema bump, updated fixtures, and full dependent verification.
